import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../api';import {inr} from '../fmt';
export default function AdminDashboard(){const [stats,setStats]=useState(),[orders,setOrders]=useState([]);const load=()=>{api.get('/admin/stats').then(r=>setStats(r.data));api.get('/admin/orders').then(r=>setOrders(r.data.orders))};useEffect(load,[]);const status=async(id,s)=>{await api.patch(`/admin/orders/${id}/status`,{status:s,note:'Updated by OxStore studio'});load()};return <main className="section"><div className="section-title"><h1>Studio dashboard</h1><Link className="button" to="/admin/products">Manage products</Link></div>{stats&&<div className="stats">{[['Revenue',inr(stats.revenue)],['Paid orders',stats.orders],['Clients',stats.users],['Products',stats.products]].map(([a,b])=><div key={a}><small>{a}</small><b>{b}</b></div>)}</div>}<h2>Recent orders</h2><div className="admin-orders">{orders.map(o=><div key={o._id}><span><b>{o.user?.name}</b><small>#{o._id.slice(-7)} · {inr(o.amount)}</small></span><select value={o.status} onChange={e=>status(o._id,e.target.value)}>{['processing','confirmed','shipped','delivered','cancelled'].map(s=><option key={s}>{s}</option>)}</select></div>)}</div></main>}
