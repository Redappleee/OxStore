import {Link} from 'react-router-dom';import {useWishlist} from '../context/WishlistContext';
import {inr} from '../fmt';export default function ProductCard({product}){const {wishlist,toggle}=useWishlist(),saved=wishlist.products?.some(p=>(p._id||p)===product._id);return <article className="product-card"><button className="heart" onClick={()=>toggle(product._id)} aria-label="Save product">{saved?'♥':'♡'}</button><Link to={`/product/${product._id}`}><img src={product.images?.[0]?.url||'https://placehold.co/800x1000?text=OXSTORE'} alt={product.name}/><div className="card-copy"><p>{product.category}</p><h3>{product.name}</h3><strong>{inr(product.price)}</strong>{product.compareAtPrice&&<del>{inr(product.compareAtPrice)}</del>}</div></Link></article>}
