const router=require('express').Router(),Stripe=require('stripe'),Razorpay=require('razorpay'),crypto=require('crypto'),Order=require('../models/Order'),{protect}=require('../middleware/auth');
const stripe=Stripe(process.env.STRIPE_SECRET_KEY||'sk_test_missing');
router.post('/intent',protect,async(req,res)=>{const order=await Order.findById(req.body.orderId);if(!order||!order.user.equals(req.user.id))return res.status(404).json({message:'Order not found'});if(order.paymentStatus==='paid')return res.status(400).json({message:'Order already paid'});const intent=await stripe.paymentIntents.create({amount:Math.round(order.amount*100),currency:order.currency,metadata:{orderId:order.id}});order.paymentProvider='stripe';order.paymentIntentId=intent.id;await order.save();res.json({clientSecret:intent.client_secret})});
router.post('/razorpay/order',protect,async(req,res)=>{if(!process.env.RAZORPAY_KEY_ID||!process.env.RAZORPAY_KEY_SECRET)return res.status(503).json({message:'Razorpay is not configured'});const order=await Order.findById(req.body.orderId);if(!order||!order.user.equals(req.user.id))return res.status(404).json({message:'Order not found'});if(order.paymentStatus==='paid')return res.status(400).json({message:'Order already paid'});order.currency='inr';await order.save();const razorpay=new Razorpay({key_id:process.env.RAZORPAY_KEY_ID,key_secret:process.env.RAZORPAY_KEY_SECRET});const gatewayOrder=await razorpay.orders.create({amount:Math.round(order.amount*100),currency:'INR',receipt:`ox_${order.id.slice(-20)}`,notes:{oxstoreOrderId:order.id}});order.paymentProvider='razorpay';order.razorpayOrderId=gatewayOrder.id;await order.save();res.json({keyId:process.env.RAZORPAY_KEY_ID,gatewayOrder:{id:gatewayOrder.id,amount:gatewayOrder.amount,currency:gatewayOrder.currency},orderId:order.id})});
router.post('/razorpay/verify',protect,async(req,res)=>{const {orderId,razorpay_payment_id,razorpay_order_id,razorpay_signature}=req.body;const order=await Order.findById(orderId);if(!order||!order.user.equals(req.user.id)||order.razorpayOrderId!==razorpay_order_id)return res.status(400).json({message:'Invalid Razorpay order'});const expected=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET).update(`${order.razorpayOrderId}|${razorpay_payment_id}`).digest('hex');const valid=razorpay_signature&&expected.length===razorpay_signature.length&&crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(razorpay_signature));if(!valid)return res.status(400).json({message:'Payment signature verification failed'});order.paymentStatus='paid';order.status='confirmed';order.razorpayPaymentId=razorpay_payment_id;order.timeline.push({status:'confirmed',note:'Razorpay payment verified'});await order.save();res.json({message:'Payment verified',order})});
router.post('/webhook',async(req,res)=>{let event;try{event=stripe.webhooks.constructEvent(req.body,req.headers['stripe-signature'],process.env.STRIPE_WEBHOOK_SECRET)}catch(e){return res.status(400).send(`Webhook error: ${e.message}`)}if(event.type==='payment_intent.succeeded'){const id=event.data.object.metadata.orderId;await Order.findByIdAndUpdate(id,{paymentStatus:'paid',status:'confirmed',$push:{timeline:{status:'confirmed',note:'Payment received'}}});}if(event.type==='payment_intent.payment_failed')await Order.findOneAndUpdate({paymentIntentId:event.data.object.id},{paymentStatus:'failed'});res.json({received:true})});
const razorpayWebhook=async(req,res)=>{const signature=req.headers['x-razorpay-signature'],secret=process.env.RAZORPAY_WEBHOOK_SECRET;if(!signature||!secret)return res.status(400).json({message:'Invalid Razorpay webhook'});const expected=crypto.createHmac('sha256',secret).update(req.body).digest('hex');if(expected.length!==signature.length||!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(signature)))return res.status(400).json({message:'Invalid Razorpay webhook signature'});const event=JSON.parse(req.body.toString('utf8'));if(['payment.captured','order.paid'].includes(event.event)){const payment=event.payload.payment?.entity;const razorpayOrderId=payment?.order_id||event.payload.order?.entity?.id;if(razorpayOrderId)await Order.findOneAndUpdate({razorpayOrderId,paymentStatus:{$ne:'paid'}},{paymentStatus:'paid',status:'confirmed',razorpayPaymentId:payment?.id,$push:{timeline:{status:'confirmed',note:'Razorpay webhook confirmed payment'}}});}res.json({received:true})};
module.exports={router,webhook:router,razorpayWebhook};
